# ~/.bash_profile

[ -f "$HOME/.bashrc" ] && . "$HOME/.bashrc"
[ -d "$HOME/go" ] && GOPATH="$HOME/go" && export GOPATH

PATH="$GOPATH/bin:$PATH"
PATH="$HOME/.local/bin:$HOME/bin:$GOPATH/bin:/usr/local/bin:$PATH"
export PATH
