# ~/.bash_profile

test -f "$HOME/.bashrc" && . "$HOME/.bashrc"

test -d "$HOME/go" && GOPATH="$HOME/go"
export GOPATH

PATH="$HOME/.local/bin:$HOME/bin:/usr/local/bin:$PATH"
PATH="$GOPATH/bin:$PATH"
export PATH
