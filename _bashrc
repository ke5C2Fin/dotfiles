# If not running interactively, do not do anything
[[ $- != *i* ]] && return
#[[ -z "$TMUX" ]] && exec tmux

[ -f ~/.bash_aliases ] && . ~/.bash_aliases

shopt -s histappend

# Completion
complete -cf sudo
complete -cf man

